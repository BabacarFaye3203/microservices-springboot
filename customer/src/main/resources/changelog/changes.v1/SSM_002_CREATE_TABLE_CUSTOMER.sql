CREATE TABLE customer (
                          id SERIAL PRIMARY KEY,
                          uuid VARCHAR(255),
                          name VARCHAR(255),
                          address VARCHAR(255),
                          email VARCHAR(255)
);
